import React from 'react';

// Default layout component to prevent build errors
// This is a simple pass-through that preserves the original functionality
export default function DocItemLayout({ children }) {
  return <>{children}</>;
}